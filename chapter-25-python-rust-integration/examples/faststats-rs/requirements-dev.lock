# Exact direct Python development-tool inputs for the recorded Linux run.
# This is not a cross-platform/hermetic resolver lock: transitive dependencies
# and hashes are not recorded. Cargo.lock separately locks Rust dependencies.
maturin==1.14.1
mypy==2.2.0
pytest==9.1.1
