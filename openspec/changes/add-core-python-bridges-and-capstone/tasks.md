## 1. OpenSpec contracts and implementation boundary

- [x] 1.1 Strictly validate the proposal, design, seven delta specs, and task graph; resolve contradictions before content work. Validation: `openspec validate add-core-python-bridges-and-capstone --strict`. Risk/scope: planning files only; do not alter unrelated active-change ownership.
- [x] 1.2 Capture the apply instructions and confirm that implementation is limited to the three prerequisite bridges, Chapters 26–28, their explicit evidence, navigation, curriculum, and additive 30/120 parity migration. Validation: `openspec status --change add-core-python-bridges-and-capstone --json`. Risk/scope: deferred security, general concurrency, and profiling remain excluded.

## 2. Beginner prerequisite bridges

- [x] 2.1 Extend canonical Chapter 1 with a prerequisite-free edit/save/run/diagnose/repair loop and update its objectives, route, exercises, mistakes, solution, checkpoint, and reflection. Validation: bounded normal and `NameError` observations plus generic Markdown validation. Risk/scope: no editor-specific dependency or system-directory write.
- [x] 2.2 Extend canonical Chapter 2 with `bool`, comparisons, `None`, identity, false-like values, incompatible-order recovery, and absolute-tolerance float comparison; keep `math.isclose` optional. Validation: bounded normal and expected-error observations. Risk/scope: no hidden conditional/import prerequisite.
- [x] 2.3 Extend canonical Chapter 7 with standard/local/third-party module distinctions, both import forms, `python -m`, missing-module diagnosis, shadowing recovery, and an updated essential checkpoint. Validation: isolated temporary-directory subprocess checks. Risk/scope: never mutate the interpreter installation or install `collections`.
- [x] 2.4 Translate the Chapter 1 bridge semantically into Spanish, Catalan, Swedish, and Arabic with matching technical behavior and localized continuation links. Validation: selectors, fences, links, headings, and Arabic wrapper checks. Risk/scope: human fluency and bidi approval remain pending.
- [x] 2.5 Translate the Chapter 2 bridge semantically into Spanish, Catalan, Swedish, and Arabic with unchanged values, operators, outcomes, TODOs, recovery, and rubric. Validation: structural/parity checks and executable canonical contract. Risk/scope: human fluency remains pending.
- [x] 2.6 Translate the Chapter 7 bridge semantically into Spanish, Catalan, Swedish, and Arabic with unchanged commands, module names, diagnostics, recovery, and checkpoint. Validation: structural/parity checks and isolated canonical contract. Risk/scope: human fluency remains pending.

## 3. Chapter 26 iteration and generators

- [x] 3.1 Implement `examples/iteration_pipeline.py` with strict pairing, countdown, bounded squares, delegation, delayed failure, and explicit cleanup contracts without import-time work. Validation: direct imports and unit tests. Risk/scope: standard library, finite inputs, and fake resources only.
- [x] 3.2 Add Chapter 26 normal, empty, unequal, invalid-bound, exhaustion, delayed-failure, recovery, and cleanup tests plus the declared deterministic discovery command. Validation: `python -B -m unittest discover -s chapter-26-iteration-generators/examples/tests -t chapter-26-iteration-generators/examples -p 'test_*.py'`. Risk/scope: bounded execution and no residue.
- [x] 3.3 Write canonical Chapter 26 essential route for loop-derived comprehensions, `enumerate`, strict/non-strict `zip`, predictions, TODOs, recovery, solution, checkpoint, and rubric. Validation: companion tests and generic validator. Risk/scope: no nested/side-effecting essential comprehensions.
- [x] 3.4 Write canonical Chapter 26 professional route for iterable/iterator identity, state, `iter`, `next`, defaults, exhaustion, one-shot use, mutation warning, recovery, and assessment. Validation: stable exception-category evidence and companion tests. Risk/scope: no custom iterator-class requirement.
- [x] 3.5 Write canonical Chapter 26 optional advanced route for generator expressions/functions, bounded laziness, delayed errors, `yield from`, `return`, and deterministic cleanup. Validation: companion tests and timeout-bounded snippet evidence. Risk/scope: no unbounded consumption or universal performance claim.
- [x] 3.6 Add Chapter 26 traceability mapping from every objective to teaching, TODO/hint, solution, checkpoint/rubric, source reference, and test. Validation: every required row resolves to an existing anchor/path. Risk/scope: executable authority remains the companion.
- [x] 3.7 Publish Spanish, Catalan, Swedish, and Arabic Chapter 26 siblings with equivalent routes, examples, recovery, assessments, links, and source references. Validation: selector/heading/fence/link/RTL/parity checks. Risk/scope: human linguistic/accessibility/bidi acceptance remains pending.

## 4. Chapter 27 gradual typing

- [x] 4.1 Implement `examples/typed_inventory.py` with `InventoryRow`, `PriceSource`, `parse_row`, `first_matching`, `Inventory.add -> Self`, exact validation bounds, and non-mutation guarantees. Validation: direct import and runtime unit tests. Risk/scope: annotations do not masquerade as runtime enforcement.
- [x] 4.2 Add runtime tests plus checker-positive, checker-negative, corrected consumers, exact development-tool pins, and a bounded runtime/checker verifier with explicit prerequisite-missing failure. Validation: runtime mode always; checker mode when the declared pin is provisioned. Risk/scope: no implicit install or network access.
- [x] 4.3 Write canonical Chapter 27 essential route for annotations, typed collections, unions, `None` narrowing, runtime behavior, explicit boundary validation, TODOs, solution, checkpoint, and rubric. Validation: runtime verifier and generic validator. Risk/scope: professional constructs remain optional.
- [x] 4.4 Write canonical Chapter 27 professional and optional checker routes for `Callable`, `TypedDict`, structural `Protocol`, `Self`, basic generics, stable diagnostic categories, recovery, and stopping points. Validation: source references and verifier evidence. Risk/scope: no broad `Any`, unqualified ignore, or diagnostic golden prose.
- [x] 4.5 Add Chapter 27 traceability and verifier documentation with truthful direct-pin, checker-host, cache, acquisition, and cleanup claims. Validation: all referenced paths/anchors exist and runtime verification is repository-clean. Risk/scope: checker compatibility is limited to observed evidence.
- [x] 4.6 Publish Spanish, Catalan, Swedish, and Arabic Chapter 27 siblings with equivalent static/runtime distinctions, identifiers, cases, assessments, links, and source references. Validation: selector/heading/fence/link/RTL/parity checks. Risk/scope: human linguistic/accessibility/bidi acceptance remains pending.

## 5. Chapter 28 professional capstone companion

- [x] 5.1 Scaffold the `course-order-tracker` pure-Python `src` project with metadata, readme, license, standard-library-only runtime, `order_tracker` import package, and `order-tracker` entry point. Validation: metadata parsing and clean source import tests. Risk/scope: no index upload or generated artifact in the repository.
- [x] 5.2 Implement immutable order values, validation, stable domain exceptions, in-memory repository, deterministic service lifecycle, and unchanged-state recovery. Validation: normal/boundary/invalid/recovery unit and contract tests. Risk/scope: synthetic data only.
- [x] 5.3 Implement the SQLite repository with idempotent schema, bounded busy timeout, explicit transactions, deterministic ordering, exception mapping, and rollback preservation. Validation: shared in-memory/SQLite contract tests in temporary directories. Risk/scope: no database outside an explicitly selected path.
- [x] 5.4 Implement CLI/configuration/logging with exact precedence, commands, stable stdout/stderr, exit codes 0/1/2, privacy-safe events, and no implicit database. Validation: bounded subprocess tests from a foreign working directory. Risk/scope: no traceback or sensitive/path disclosure for expected errors.
- [x] 5.5 Implement the optional bounded loopback adapter reusing the service contract, OS-assigned port, request/concurrency/time limits, and deterministic shutdown/cancellation. Validation: bounded localhost normal/limit/recovery tests. Risk/scope: no public bind, TLS/auth/deployment claim, or packaging dependency.
- [x] 5.6 Add complete domain, repository, service, CLI, configuration, logging, and optional adapter tests with temporary state and residue checks. Validation: one documented standard-library test command. Risk/scope: subprocess output/time are bounded.
- [x] 5.7 Implement `tools/verify_artifact.py` for isolated source snapshot, sdist/wheel build and inspection, wheel rebuild from sdist, clean install, `pip check`, metadata/import/domain/CLI smoke from a foreign directory, SHA-256 report, and cleanup. Validation: accepted verifier command with explicitly provisioned build inputs. Risk/scope: failure is non-zero and never disables isolation or publishes.

## 6. Chapter 28 learning and assessment

- [x] 6.1 Write canonical Chapter 28 foundation stage with prerequisite trace, prediction, in-memory domain TODO/hint, normal/boundary/error/recovery paths, solution, checkpoint, rubric, and stopping point. Validation: mapped companion tests and generic validator. Risk/scope: no persistence, networking, or packaging prerequisite.
- [x] 6.2 Write canonical Chapter 28 practical stage for SQLite, CLI, configuration, logging, tests, transactions, recovery, checkpoint, rubric, and stopping point. Validation: mapped companion subprocess/contract tests. Risk/scope: no new framework or production service.
- [x] 6.3 Write canonical Chapter 28 optional systems and hero packaging stages with exact bounds, artifact-versus-source evidence, failure recovery, publication prohibition, checkpoints, and rubrics. Validation: localhost tests and artifact verifier or explicit prerequisite-missing evidence. Risk/scope: optional stages cannot hide practical failures.
- [x] 6.4 Add Chapter 28 traceability mapping each feature to a prior checkpoint and observable test/manual rubric item, plus complete companion verification instructions. Validation: every required feature has a taught prerequisite and evidence target. Risk/scope: no first-time concept in the final solution.
- [x] 6.5 Publish Spanish, Catalan, Swedish, and Arabic Chapter 28 siblings with equivalent stages, identifiers, commands, outputs, bounds, recovery, assessments, and links. Validation: selector/heading/fence/link/RTL/parity checks. Risk/scope: human linguistic/accessibility/bidi acceptance remains pending.

## 7. Explicit learning-bridges quality evidence

- [x] 7.1 Add a least-privilege `learning-bridges` book plugin that runs the disposable Chapter 1/2/7 contracts, declared Chapter 26–28 source suites, Chapter 27 checker preflight, and Chapter 28 artifact-prerequisite preflight while validating registered source contracts. Validation: plugin unit tests and selected profile. Risk/scope: full artifact verification remains explicit; no arbitrary learner code, implicit install, or network.
- [x] 7.2 Register `learning:contract` source references, the plugin check ID, the explicit quality-matrix check/profile, the handoff selection, and the plugin path in the publication-signoff quality digest without weakening fixed runner contracts. Validation: validator/runner/parity-signoff tests and source-reference checks. Risk/scope: generic validation remains domain-neutral.
- [x] 7.3 Extend the least-privilege CI workflow with the isolated `learning-bridges` profile and no write permissions or hidden dependency acquisition. Validation: workflow/config tests and local profile run. Risk/scope: human gates remain outside CI.

## 8. Curriculum and multilingual navigation

- [x] 8.1 Extend `tools/curriculum_map.toml` with iteration/generator, gradual-typing, and longitudinal-capstone concepts/checkpoints plus explicit non-numerical route placement and prerequisites. Validation: `python -B tools/validate_curriculum.py`. Risk/scope: graph remains acyclic and existing path IDs stay stable.
- [x] 8.2 Update canonical root navigation with Chapters 26–28 in catalogue order and independently stoppable pedagogical routes, then copy it byte-for-byte to `README.en.md`. Validation: byte comparison and root-link checks. Risk/scope: add links only after all targets exist.
- [x] 8.3 Update Spanish, Catalan, Swedish, and Arabic root indexes with language-matched links, route sequence, outcomes, completion criteria, duration, and stopping points. Validation: all six root indexes and target siblings pass navigation checks. Risk/scope: route meaning is equivalent even when numerical order differs.

## 9. Parity topology and publication evidence

- [x] 9.1 Add all 15 new Markdown siblings to discovered publication scope and explicitly reconcile schema-v2 parity topology from 27/108 to exactly 30/120. Validation: default parity validation reports 30/120. Risk/scope: explicit write only after the content tree is stable.
- [x] 9.2 Verify additive reconciliation preserves byte-identical unrelated human fields, invalidates only changed Chapter 1/2/7 and root-bound evidence, and creates pending leaves for Chapters 26–28. Validation: parity transition tests and focused before/after digest comparison. Risk/scope: never synthesize reviewer roles, dates, notes, or acceptance.
- [x] 9.3 Record current automated signals, emit current review packets, and regenerate the root publication-signoff consumer as pending for the new digests. Validation: read-only parity/signoff commands accept structure and `--require-accepted` fails for the documented human gates. Risk/scope: generated evidence is not human approval.
- [ ] 9.4 Reconcile exact-count planning text in overlapping active changes only after showing the proposed artifact revisions and obtaining the required explicit confirmation. Validation: all active OpenSpec changes are mutually coherent and strict-valid. Risk/scope: do not edit another change's implementation ownership without confirmation.

## 10. Verification, human gates, and handoff

- [x] 10.1 Run Chapter 1/2/7 isolated observations, Chapter 26 tests, Chapter 27 runtime/checker evidence as provisioned, Chapter 28 full tests, and artifact verification; record any unavailable optional prerequisite honestly. Validation: every selected automated command passes or has the specified non-pass prerequisite state. Risk/scope: cleanup assertions pass.
- [x] 10.2 Run tooling unit tests, curriculum validation, parity validation, generic book validation, all domain profiles, the learning-bridges profile, strict OpenSpec validation, whitespace checks, ignored-inclusive hygiene scans, and `git diff --check`. Validation: no unexplained regression or residue. Risk/scope: existing honest human-review warnings are not suppressed.
- [x] 10.3 Review the final diff for stable paths/anchors, English mirror identity, localized navigation, scope containment, attribution/provenance claims, and absence of secrets or generated artifacts. Validation: maintainer-readable diff summary and clean status apart from intentional files. Risk/scope: preserve unrelated user changes.
- [ ] 10.4 Obtain competent linguistic and semantic review for Spanish, Catalan, Swedish, and Arabic changes, bound to current digests. Validation: reviewer evidence is recorded through the approved parity transition. Risk/scope: remains unchecked until a human reviewer acts.
- [ ] 10.5 Obtain competent rendered-accessibility and Arabic bidirectional review for every changed/new unit and root index, bound to current digests. Validation: reviewer evidence records the rendered targets and outcome. Risk/scope: remains unchecked until a human reviewer acts.
- [ ] 10.6 Obtain competent provenance/license and final publication sign-offs for the expanded current tree. Validation: accepted source/locale/root evidence and publication gate. Risk/scope: remains unchecked until authorized humans act.
- [x] 10.7 Commit planning, content/companions, localization/navigation, and evidence/tooling in logical non-destructive commits, then push the current branch to every authenticated requested remote and report any remote authentication blocker exactly. Validation: commit log, clean worktree, and remote push results. Risk/scope: no force push or history rewrite.
