## 1. Planning, Baseline, And Ownership

- [x] 1.1 Validate the reduced OpenSpec contract. Objective: prove proposal, design, five delta specs, and this 48-task graph describe the same 10–15 hour branch-and-join appendix. Deliverables: strict-valid planning artifacts only. Validation: `openspec validate add-software-design-patterns-appendix --strict`, `openspec validate --all --strict`, `openspec doctor`, and scenario/task trace review. Risk: do not report Appendix C as fully published. Scope: S.
- [x] 1.2 Capture apply instructions and implemented truth. Objective: distinguish the discovery-visible `.gitkeep` baseline and unrelated dirty work from later regressions. Deliverables: current CPython, unit/parity counts, active changes, profiles, worktree owners, and resolved apply context. Validation: `openspec instructions apply --change add-software-design-patterns-appendix --json` plus read-only curriculum/parity/quality/status commands. Risk: never baseline away a real failure or overwrite another change. Scope: S.
- [ ] 1.3 Resolve overlapping spec ownership before shared work or sync. Objective: make the base specs truthfully own review-schema/root contracts, Core 30/120, Environment 32/128, and existing Learning/Environment profiles before Appendix C owns only 33/132 and `patterns-domain`. Deliverables: maintainer-approved sync/rebase/reconciliation order and an updated Appendix C delta if the base changes. Validation: cumulative scenario comparison, current digests/counts, and strict validation show no contract loss or silently completed foreign task. Risk: hard human coordination gate for shared quality/parity/root files; appendix-local work may proceed. Scope: human gate.
- [ ] 1.4 Re-read shared surfaces before every shared edit wave. Objective: integrate with the then-current root, workflow, quality, parity, attribution, and sign-off state. Deliverables: refreshed status/digest inventory and conflict-free edit sequence. Validation: focused diffs and before-digests match the approved owners from task 1.3. Risk: stop and update the plan if counts, owners, or contracts drift. Scope: S.

## 2. Direct Job Runner And Core Routes

- [x] 2.1 Replace the scaffold with an import-safe direct companion. Objective: establish one small `Job`/`Result` vocabulary and passing direct behavior without a hidden `dataclass` prerequisite. Deliverables: `domain.py`, `direct.py`, test package, route recovery point, and no import-time work. Validation: CPython 3.11+ imports plus happy, boundary, invalid, unchanged-state, recovery, and residue tests. Risk: standard library and synthetic data only. Scope: S.
- [x] 2.2 Implement the decision-record and transfer fixtures. Objective: make pattern selection—not structural copying—the common assessed skill. Deliverables: stable problem/forces/simple-option/pattern/cost/failure/test/removal model and a second-domain fixture of roughly twenty lines. Validation: a justified direct solution and one justified pattern solution both satisfy observable tests. Risk: no new capstone vocabulary. Scope: S.
- [x] 2.3 Implement Essential Strategy, function Factory, and Adapter. Objective: separate policy, construction choice, and one incompatible collaborator boundary only after their pressures appear. Deliverables: `essential.py`, two policies, validating construction, legacy fake, explicit error conversion, and tests. Validation: direct and refactored contracts match; unknown selection and adapted failure recover deterministically. Risk: no networking, `typing.Protocol`, hierarchy, or third-party dependency. Scope: M.
- [x] 2.4 Implement Professional Decorator and bounded Observer. Objective: add one cross-cutting observation and removable synchronous notifications without changing the job contract. Deliverables: `professional.py`, observable wrapper ordering, capped subscriptions, stable iteration, failure policy, and idempotent unsubscribe. Validation: wrapped return/failure equivalence plus cap, mutation-during-notify, observer failure, and zero-history tests. Risk: do not label `@decorator` syntax or a callback list as the pattern intent or a network bus. Scope: M.
- [x] 2.5 Implement Advanced composition and ports. Objective: make time, identifiers, output, and execution explicitly owned without globals or frameworks. Deliverables: `architecture.py`, composition root, one execution port, deterministic fakes, and in-memory Adapter. Validation: changing adapters leaves domain behavior/tests unchanged and core modules import no process-global collaborator. Risk: no database, Repository duplicate, Facade implementation, or second service layer. Scope: M.
- [x] 2.6 Lock the complete core contract and recovery points. Objective: prove Essential, Professional, Advanced, and transfer outcomes before prose. Deliverables: `test_core_patterns.py`, stable test/source IDs, runnable starters, and safe route exits. Validation: focused suite passes offline with normal/boundary/recovery cases and no card-only pattern implementation. Risk: learner-facing explanations and rubrics remain task 6 work. Scope: S.

## 3. Network Resilience Policy

- [x] 3.1 Implement the closed failure taxonomy, injected time, and deduplicating fake. Objective: distinguish transient/permanent/local/cancellation outcomes and prove that a key alone is not retry safety. Deliverables: `resilience.py` errors/results, monotonic clock/sleeper, stable-key fake, and one-effect evidence. Validation: removing dependency-side enforcement disables automatic retry; caller cancellation propagates. Risk: no wall-clock sleep, global patch, thread, real effect, or unseen Capacity concept. Scope: M.
- [x] 3.2 Implement Retry under one total deadline. Objective: enforce at most three attempts, 100 ms per-attempt maximum, injected 50/100 ms backoff, and 500 ms total budget for attempts/backoff. Deliverables: retry policy and tests for recovery, conditional unsafe/permanent one-call behavior, exhaustion cause, full-backoff-fits budget edge, stalled-attempt cancellation/await, and no late call. Validation: exact calls/delays and `attempt_timeout` mapping pass with fake time. Risk: no partial backoff, fourth attempt, swallowed `CancelledError`, or Bulkhead prerequisite. Scope: M.
- [x] 3.3 Implement Circuit Breaker as the State example. Objective: apply consistent positive, negative, and neutral dependency-health observations across CLOSED/OPEN/HALF_OPEN. Deliverables: one breaker per fake dependency, stable `circuit_open`, three-negative opening, positive reset, exclusive single-attempt probe, raw/transient and exhausted-transient classification, and cancellation semantics. Validation: every logical operation contributes at most one observation; unsafe raw transient can open after three calls; permanent response resets while propagating; cancelled probe creates no new cooldown. Risk: no per-request, cross-dependency, or Capacity state. Scope: M.
- [x] 3.4 Verify independently complete resilience composition. Objective: prove `validation → breaker → retry → timeout → dependency` and final cleanup before Capacity exists. Deliverables: integration tests for nine-call opening, positive reset, open zero-call behavior, deadline, cancellation, and zero owned work. Validation: focused resilience cases pass deterministically with no Bulkhead import or fixture. Risk: Capacity extends this seam only in task 4.4. Scope: M.

## 4. Capacity, Optional Pub/Sub, And Existing-Code Crosswalk

- [x] 4.1 Implement two simplified Bulkheads. Objective: demonstrate real `local`/`remote` failure-domain isolation with the smallest capacity model. Deliverables: `capacity.py`, one execution lease and one queued slot per compartment, immediate `overloaded`, injected `admission_timeout`, promotion, idempotent removal, and prior-transient cause preservation. Validation: saturated `remote` rejects while `local` completes; expiry before a first call is neutral/local, expiry after a transient yields one `RetryExhausted`, and cancellation leaves no waiter or lease. Risk: no workers, second waiter queue, fairness claim, or timing guess. Scope: M.
- [x] 4.2 Implement optional enqueue-only Pub/Sub. Objective: contrast synchronous Observer with bounded fan-out without making delivery claims. Deliverables: `pubsub.py`, four-subscriber/eight-event caps, `enqueued`/`disconnected` IDs, `slow_subscriber`, discarded count, per-subscriber FIFO, and idempotent removal. Validation: full subscriber isolation, fifth registration recovery, no content leak, no history, and no background task pass. Risk: no broker, acknowledgement, global order, replay, or consumer-success claim. Scope: M.
- [ ] 4.3 Establish the Reactor and `TaskGroup` reading trace. Objective: reuse Chapter 23 readiness and Chapter 21 structured-concurrency ownership without adding implementation. Deliverables: exact canonical/localized anchors and rows for registration, interest, dispatch, idle close, owner, sibling and caller cancellation, `ExceptionGroup`, timeout, propagation, and cleanup; each row labels Chapter 23 plugin evidence, Chapter 21 generic/manual companion evidence, or reading-only interpretation accurately. Validation: every claim resolves to its actual source/evidence mode, caller cancellation remains visible through cleanup, and no row labels Chapter 21/23 as a nominal pattern. Risk: no selector/server/socket/Supervisor code or invented Chapter 21 check ID. Scope: S.
- [x] 4.4 Integrate Capacity and complete the network contract suite. Objective: insert one Bulkhead lease per attempt after the independently passing Resilience checkpoint and cover isolation, optional fan-out, cancellation, and source ownership. Deliverables: final `test_network_patterns.py` and stable evidence IDs for open-before-capacity, first-call `admission_timeout`, prior-transient exhaustion, lease-before-backoff, deadline including admission, and HALF_OPEN local rejection. Validation: all fake-time/offline cases pass, no socket API is invoked, no owned task/waiter/lease remains, and Chapter 21/23 behavior is not claimed re-executed. Risk: observed host timing is not a performance or portability guarantee. Scope: M.

## 5. Evidence Scaffolding And Minimal Plugin

- [x] 5.1 Create dated sources, traceability, and verification records. Objective: expose original work, terminology influences, licenses, route-to-test mapping, bounds, evidence ownership, and pending human gates before prose freezes. Deliverables: `SOURCES.md`, `TRACEABILITY.md`, and `VERIFICATION.md`. Validation: every planned route/check has a truthful row and unresolved provenance remains pending. Risk: do not infer permission or reviewer acceptance. Scope: S.
- [x] 5.2 Implement the minimal patterns plugin. Objective: register exactly `patterns:core-suite` and `patterns:network-suite` while delegating containment to the generic runner. Deliverables: `tools/bookcheck_plugin.py` with known standard-library test invocation and bounded diagnostics. Validation: direct registration/check tests pass under the existing 30-second/64 KiB generic contract. Risk: no second process-tree manager, output collector, snapshot, aggregate clock, socket, or implicit Chapter 23 result. Scope: M.
- [x] 5.3 Verify plugin registration and selection with synthetic fixtures. Objective: prove core/network checks remain separately selectable before READMEs and shared known-check configuration exist. Deliverables: appendix-local registration, not-selected, and failure fixtures only. Validation: the two plugin checks register and execute independently without claiming that future lesson source references already validate. Risk: real path/check ownership validation waits for task 8.2 after ownership gate 1.3. Scope: S.

## 6. Canonical English Appendix C

- [x] 6.1 Write the opening, glossary, routes, and dual indexes. Objective: introduce the job story, decision record, exact prerequisites, 10–15 hour branch/join budget, family/problem navigation, microcycles, recovery points, and safe exits before pattern detail. Deliverables: canonical `README.md` shell with one H1; the exact pattern/problem/family/route/status/simple-option/checkpoint row schema; accessible graph/table/list equivalents; and the callable/composition/exception/module/test entry self-assessment with recovery links. Validation: route graph and entry diagnostic find no mini-bridge, hidden prerequisite, “zero to hero” wording, card/link ambiguity, or alphabetical teaching order. Risk: later directory expansion remains bounded by the exact closed catalogue in task 6.10. Scope: M.
- [x] 6.2 Write the Essential learning loop. Objective: teach Strategy/Factory/Adapter from tested direct pressure. Deliverables: objectives, predictions, runnable source references, a minimal Chapter 18 pytest-concepts→`unittest` syntax bridge, TODOs/hints, normal/boundary/recovery, mistakes, explained solutions, decision records, rubric, reflection, and exit. Validation: companion paths and planned check mappings exist without networking, typing, `dataclass`, or hidden test-framework knowledge; configured source-reference acceptance remains pending until task 8.2. Risk: keep two-session cognitive load. Scope: L.
- [x] 6.3 Write the Professional learning loop. Objective: teach Decorator/Observer and Python-specific contrasts while cards remain recognition tools. Deliverables: executable cases and decision records; exact Command/Singleton/Proxy/Template Method cards; and the transfer starter, prompt, hint, normal/boundary/recovery, explained solution, rubric, reflection, and exit. Validation: every executable/card decision names pressure, simpler alternative, cost, verification, and removal threshold; transfer recovery is available before stopping; no card-only contrast becomes a route-level implementation. Risk: keep the four cards compact; avoid pattern worship and callback-bus overclaims. Scope: L.
- [x] 6.4 Write the Advanced learning loop. Objective: teach composition root, dependency injection, port, and in-memory Adapter as testability boundaries. Deliverables: route starter/recovery point, ownership explanation, deterministic fakes, guided modification, normal/boundary/recovery, explained solution, decision record with simpler/no-pattern alternative, Facade card, rubric, Chapter 17/28 links, and exit. Validation: the rubric accepts justified direct composition, companion contracts resolve, and no framework/database/Repository duplicate appears. Risk: keep Advanced independently optional. Scope: L.
- [x] 6.5 Write the three-session Network resilience learning loop. Objective: teach Retry safety/deduplication first and Circuit Breaker State second, then compose them with consistent health semantics. Deliverables: route starter/recovery point, Chapter 19 critique, Chapter 21/23 links, timelines plus text, predictions, TODOs/hints, normal/boundary/recovery, explained solutions, exact bounds, State contrast, decision record, rubric, and stop. Validation: the independently complete resilience suite proves key-without-enforcement rejection, deadline/cancellation, raw/exhausted transient observations, and exclusive probe without Capacity. Risk: no production recipe, retry storm, or hidden Bulkhead concept. Scope: L.
- [x] 6.6 Write Capacity and optional Pub/Sub. Objective: teach isolation/admission in one session and budget Pub/Sub as the separately skippable second session. Deliverables: route starter/recovery point, two-compartment timeline/ownership map, “do not confuse” table/list, predictions, TODOs/hints, normal/boundary/recovery, explained solutions, decision record with simpler semaphore/fail-fast/no-pattern alternatives, saturation lab, optional fan-out extension, rubric, and stop. Validation: the required checkpoint grades Bulkhead isolation/admission without Pub/Sub; the rubric accepts justified simpler capacity control; optional tests prove subscriber recovery and `enqueued` is never called delivered. Risk: no backpressure, broker, fairness, or durability claim. Scope: L.
- [x] 6.7 Write the final crosswalk and synthesis. Objective: trace Reactor-like and `TaskGroup` responsibilities, then make one cross-route design decision. Deliverables: route worksheet starter and answer key, exact Chapter 21/23 source map, caller-cancellation trace, textual ownership alternative, decision record for the simpler streams/TaskGroup choice, longitudinal synthesis, normal/boundary/recovery reading cases, final rubric, and reflection. Validation: no new network implementation, deferred Professional solution, or first-time assessed concept; all anchors and evidence owners resolve. Risk: “Supervisor-like” remains comparison only. Scope: M.
- [x] 6.8 Validate the revised Appendix C organization contract. Objective: replace the former monolithic-path assumption with the requested portable `zz_` path, stable logical ID, closed catalogue, and per-entry documentation/example rules before implementation. Deliverables: coherent proposal, design, delta specs, and tasks. Validation: strict change/all validation and doctor pass with no stale path or contradictory card/cross-link requirement. Risk: do not edit implementation until the contract is coherent. Scope: S.
- [x] 6.9 Migrate the Appendix-local tree without losing tested behavior. Objective: move Appendix C to `zz_Appendix C Software Design Patterns`, relocate the route-level job-runner spine under `_shared/patterns`, and rewrite every Appendix-local path/command/plugin reference. Deliverables: portable tree with no colon, preserved core/network/plugin tests, and explicit old→new migration note. Validation: all 68 route tests plus 7 catalogue tests and 8 plugin tests pass from the quoted new path and imports perform no work. Risk: no claim that Git hosting redirects old blob URLs; shared discovery waits for task 8.2. Scope: M.
- [x] 6.10 Build the closed per-pattern catalogue and family table. Objective: give each of the exact 25 included patterns or crosswalk techniques one `<family> - <Pattern Name>` directory and make the root table the complete family-grouped lookup. Deliverables: `catalog.toml`, 25 linked root rows with basic explanations, and 25 directories each containing `README.md` plus `example.py`. Validation: manifest↔table↔filesystem bijection passes; Builder/Composite remain explicitly omitted. Risk: spaces/hyphens are display paths, never Python package names. Scope: L.
- [x] 6.11 Verify every local pattern learning loop. Objective: make executable entries runnable, cards small verified contrasts, and cross-links bounded clients/readings without changing their status or duplicating source owners. Deliverables: path loader, catalogue tests, per-example smoke/behavior cases, and updated traceability/verification records. Validation: every example is bounded, offline, import-safe, and has normal/boundary/recovery evidence appropriate to its status; core/network suites remain separately selectable. Risk: no catalogue inflation or false nominal-pattern claim. Scope: L.
- [ ] 6.12 Complete canonical QA, close appendix-local evidence records, and obtain the cold-read pilot. Objective: correct cognitive load, jargon, blocked steps, timing, recovery, stale anchors, and planned-only trace rows before translation. Deliverables: frozen canonical root and nested pages, executable-fence audit, final `TRACEABILITY.md`/`VERIFICATION.md` anchors, and role-only/no-PII records from non-design target-profile participants naming prerequisites, environment, route coverage, documented versus unplanned help, time, findings, corrections, and retests. Validation: objectives trace to examples/exercises/checks; participants collectively cover all six checkpoints within published upper estimates using only documented help; all local paths/anchors resolve; every over-time/unplanned-help or other critical finding is corrected and retested. Risk: automation cannot perform this pilot or mark it complete. Scope: human gate.

## 7. Semantic Localization And Accessibility

- [ ] 7.1 Publish Spanish root and catalogue-owned pages. Objective: provide natural Spanish with equivalent routes, pattern names, contracts, exercises, solutions, rubrics, links, safety, and non-claims. Deliverables: complete `README.es.md` root and nested siblings declared by the catalogue. Validation: semantic matrix, aggregate digest, localized links, companion identity, and planned path/check mapping pass; configured source-reference acceptance remains pending until task 8.2. Risk: linguistic approval remains pending. Scope: L.
- [ ] 7.2 Publish Catalan root and catalogue-owned pages. Objective: provide natural Catalan with the complete canonical learning contract. Deliverables: complete root and nested siblings declared by the catalogue. Validation: semantic matrix, aggregate digest, localized links, companion identity, and planned path/check mapping pass; configured source-reference acceptance remains pending until task 8.2. Risk: linguistic approval remains pending. Scope: L.
- [ ] 7.3 Publish Swedish root and catalogue-owned pages. Objective: provide natural Swedish with the complete canonical learning contract. Deliverables: complete root and nested siblings declared by the catalogue. Validation: semantic matrix, aggregate digest, localized links, companion identity, and planned path/check mapping pass; configured source-reference acceptance remains pending until task 8.2. Risk: linguistic approval remains pending. Scope: L.
- [ ] 7.4 Publish Arabic root and catalogue-owned pages. Objective: provide natural Arabic while technical literals stay LTR. Deliverables: complete root and nested siblings with one balanced outer RTL wrapper per rendered page. Validation: semantic matrix, aggregate digest, localized links, wrapper/bidi signals, companion identity, non-directional alternatives, and planned path/check mapping pass; configured source-reference acceptance remains pending until task 8.2. Risk: fluent and rendered-bidi approval remains pending. Scope: L.
- [ ] 7.5 Reconcile semantic parity and accessible rendering signals. Objective: preserve objectives, routes, identifiers, limits, cases, hints, solutions, rubrics, reflection, table/list and visual/text alternatives across five root pages and their catalogue-owned nested siblings. Deliverables: pre-packet semantic matrix and aggregate page inventory with new linguistic/accessibility decisions pending and the completed cold-read state preserved. Validation: structural checks plus manual contract comparison; no word-count proxy, premature parity packet, or automated approval. Risk: packet generation waits for task 8.4 and is not review. Scope: M.

## 8. Shared Quality, Parity, Curriculum, And Publication

- [ ] 8.1 Reconfirm the task 1.3 ownership/count/digest baseline immediately before shared mutation. Objective: stop stale cumulative deltas from overwriting review-schema, Learning, Environment, root, or parity contracts. Deliverables: current approved owners, valid 32/128/root proof, and go/no-go record. Validation: active changes and base specs match the approved order; otherwise this plan is updated first. Risk: hard gate for tasks 8.2–8.6. Scope: human gate.
- [ ] 8.2 Register Appendix C path mapping, nested-page discovery, source-reference, plugin, and attribution ownership. Objective: map physical path `zz_Appendix C Software Design Patterns` to stable logical ID `appendix-software-design-patterns`, validate its closed nested catalogue, make all root/nested `patterns:*` references valid, finalize the Appendix C `ATTRIBUTIONS.toml` entry before parity leaves, and retain Chapter 21/23 owners. Deliverables: focused book-quality/attribution configuration and tests plus explicit old→new path migration. Validation: every canonical/localized nested source reference passes together; unknown alias, duplicate ID, omitted catalogue page, unsafe/unquoted path, and wrong-owner mappings fail; provenance remains honestly `review_required`; generic domain logic is unchanged. Risk: preserve `add-book-quality-gates` ownership and baseline semantics. Scope: L.
- [ ] 8.3 Add `patterns-domain` to the closed matrix and least-privilege CI. Objective: bind the minimal plugin once with 120-second/512 KiB outer limits and one named read-only job. Deliverables: matrix/parser/selection/workflow tests and quality documentation. Validation: valid selection plus missing/tampered/duplicate/path/limit/action-ref fixtures fail closed; no secret/install/public target/elevation/waiver exists. Risk: retain every existing profile's binding and relative order. Scope: M.
- [ ] 8.4 Apply the authorized 32/128→33/132 parity transition. Objective: add only one Appendix C source and four pending locale leaves while preserving unrelated bytes and human authority before root Markdown changes. Deliverables: scoped `parity_review.py` constants/transition/help, tests, maintainer guide, current leaves/packets, and stale external sign-off input. Validation: root leaf remains byte-identical at unchanged root digests; exact before/after comparison, idempotent retry, incomplete/wrong-count failure, and `--require-accepted` honest non-pass succeed. Risk: do not make Appendix C the permanent owner of earlier Core/Environment transitions or invalidate root decisions early. Scope: M.
- [ ] 8.5 Publish curriculum nodes and all six root links atomically. Objective: add Essential branching to Professional→Advanced and Resilience→Capacity, join both at Crosswalk with exact gates/durations, then list Appendix C after A/B. Deliverables: `tools/curriculum_map.toml` and six roots. Validation: all targets/checks/leaves exist; graph is acyclic; the bridge retains Chapters 14/18 and final Chapter 23 retains 14/18/21; branch/join and 10–15 hour budget are truthful; existing prerequisite closure does not shrink; localized links and English mirror pass. Risk: no partial publication or mini-bridge node. Scope: M.
- [ ] 8.6 Reconcile the changed root leaf and refresh publication-signoff inputs. Objective: bind the six new root digests plus the already stable Appendix C/profile/provenance inputs while leaving affected decisions pending. Deliverables: reconciled root evidence, automated signals, and sign-off consumer updates without changing attribution inputs after parity reconciliation. Validation: only root decisions bound to changed digests become pending/stale, structural verification accepts current inputs, and every unperformed human decision remains non-pass. Risk: any late provenance edit requires explicit Appendix C source/locale reconciliation; automation cannot invent identity, date, note, license decision, or approval. Scope: M.

## 9. Verification And Human Handoff

- [ ] 9.1 Run focused and repository-wide machine evidence. Objective: prove core/network companions, minimal plugin, curriculum, parity, generic validation, every named profile, mirror, links, RTL signals, whitespace, and hygiene without source mutation or residue. Deliverables: versioned command/outcome record. Validation: all machine-contract checks pass or report only an explicit human-review-required state. Risk: no production, performance, or unexecuted-platform claim. Scope: M.
- [ ] 9.2 Verify implementation against this change. Objective: reconcile every requirement, scenario, design decision, task result, and rollback boundary before handoff. Deliverables: OpenSpec verification report and final diff review. Validation: strict validation, doctor, `git diff --check`, and implementation verification contain no critical/warning mismatch. Risk: do not archive or alter unrelated work. Scope: M.
- [ ] 9.3 Obtain competent linguistic and technical/pedagogical review. Objective: confirm natural Spanish/Catalan/Swedish/Arabic, semantic parity, beginner progression, cold-read recovery, pattern choices, network claims, exercises, and rubrics against current digests. Deliverables: independently recorded roles, dates, notes, and outcomes. Validation: every language and teaching dimension has an authorized current decision. Risk: keep unchecked until real reviewers act. Scope: human gate.
- [ ] 9.4 Obtain rendered-accessibility, Arabic-bidi, provenance/license, and final publication sign-offs. Objective: close remaining navigation, assistive-reading, RTL/LTR, source/adaptation, and publication decisions against current inputs. Deliverables: independent current reviewer evidence and final sign-off. Validation: accepted parity/publication gates pass without automated exceptions. Risk: keep unchecked until every competent authority actually approves. Scope: human gate.
